#!/usr/bin/env <%= Carat.settings[:shebang] || RbConfig::CONFIG["ruby_install_name"] %>
# frozen_string_literal: true

#
# This file was generated by Carat.
#
# The application '<%= executable %>' is installed as part of a gem, and
# this file is here to facilitate running it.
#

carat_binstub = File.expand_path("../carat", __FILE__)
load(carat_binstub) if File.file?(carat_binstub)

require "pathname"
ENV["CARAT_GEMFILE"] ||= File.expand_path("../<%= relative_gemfile_path %>",
  Pathname.new(__FILE__).realpath)

require "rubygems"
require "carat/setup"

load Gem.bin_path("<%= spec.name %>", "<%= executable %>")
